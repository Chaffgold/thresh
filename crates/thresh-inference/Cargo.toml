[package]
name = "thresh-inference"
description = "ONNX Runtime integration for transformer inference pipelines"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
thresh-core = { path = "../thresh-core" }
