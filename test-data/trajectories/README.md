# Trajectory samples

## `opensky-sample.parquet`

Small real-data sample for CI dry-runs of the flight-data acquisition layer
(flight-data-training-pipeline task 2.7). One row per (aircraft, instant) in
the canonical trajectory schema (`python/acquisition/schema.py`,
`TRAJECTORY_PARQUET_SCHEMA`); validated by `python/tests/test_opensky_sample.py`
in the `python-training-tree` CI lane.

| Field | Value |
|---|---|
| Source | OpenSky Network public REST `/api/states/all` (anonymous, `extended=1`) |
| Captured | 2026-07-03, 06:19:16–06:28:35 UTC (European morning departure wave) |
| Bounding box | 49.0–51.0 °N, 7.0–10.0 °E (Frankfurt / Rhein-Main area) |
| Contents | 2,101 records, 116 aircraft (median 22 observations each, ~10 s cadence) |
| Size | 72,398 bytes (budget: < 5 MB, design Decision 7) |
| SHA-256 | `5e701b2f1000b35946255d8230fc7bfb7c31e047b8f55db3b1110c87a334f452` |
| Generated by | `uv run python -m acquisition.opensky_cli --bbox 49.0 51.0 7.0 10.0 --duration-s 300` (from `python/`) |

Notes: `category` is null for most rows — few aircraft broadcast an ADS-B
emitter category, and the schema keeps it nullable. `quality_nic` /
`quality_nac_p` and `provenance` are null for the OpenSky source by design.
Timestamps are per-aircraft `time_position` fixes, so the earliest rows
slightly predate the capture window start.

## Attribution

This work uses data from the OpenSky Network. See <https://opensky-network.org/>.

> Matthias Schäfer, Martin Strohmeier, Vincent Lenders, Ivan Martinovic, and
> Matthias Wilhelm. "Bringing up OpenSky: A large-scale ADS-B sensor network
> for research." IPSN 2014.

OpenSky data is used under its academic / non-commercial terms; redistribution
posture for this repository is recorded in `LICENSING.md`.
