# Trajectory samples

## `synthetic-sample.parquet`

Wholly synthetic fixture for offline acquisition-schema checks. It uses the
canonical trajectory schema (`python/acquisition/schema.py`,
`TRAJECTORY_PARQUET_SCHEMA`) and is validated by
`python/tests/test_opensky_sample.py` in the `python-training-tree` CI lane.
The generator uses fabricated values without reading OpenSky data or calling
an external service. This fixture carries the code license, `MIT OR Apache-2.0`.

| Field | Value |
|---|---|
| Source | Fabricated trajectories generated entirely offline |
| Time range | Fixed UNIX seconds 1700000000–1700000290, stored as UTC microseconds |
| Positions | Invented eastbound tracks at latitudes 10.0, 10.01, and 10.02 degrees, starting at longitude 20.0 degrees |
| Contents | 90 records: 3 fabricated aircraft, 30 observations each, 10-second cadence |
| Identifiers | `000001`/`000002`/`000003`; callsigns `SYNTH001`/`SYNTH002`/`SYNTH003` |
| Size | 6,655 bytes (budget: < 5 MB, design Decision 7) |
| SHA-256 | `469a5f97ba4d947c9ac6b3693d3dd1282a046a9238f749fb7f951ed835f1cdbd` |
| Generated by | `uv run python -m acquisition.synthetic_fixture` (from `python/`) |

The schema's `source = "opensky"` value exercises the provider format; it does
not identify this as captured data. Every row has `provenance = {synthetic:
true}`. Parquet metadata records `thresh.fixture.origin = synthetic` and
`thresh.fixture.generator = acquisition.synthetic_fixture`. Categories cover
`A1`, `A4`, and `A7`.

This fixture validates schema and loader behavior, not live API parity or
real-data training performance. Task 2.7 remains open for authorized real-data
validation.

## Historical API fixture

On 2026-09-20, this synthetic fixture replaced `opensky-sample.parquet`, a
2,101-record capture of OpenSky's public REST `/api/states/all` endpoint over
the Frankfurt region on 2026-07-03. No redistribution permission is known to
the maintainer. The earlier file remains recoverable from git history;
replacement does not grant permission to use or redistribute that data.

The historical capture was attributed to the OpenSky Network:

> Matthias Schäfer, Martin Strohmeier, Vincent Lenders, Ivan Martinovic, and
> Matthias Wilhelm. "Bringing up OpenSky: A large-scale ADS-B sensor network
> for research." IPSN 2014.

Attribution alone does not authorize redistribution. The next real-data
acquisition/training run is pending confirmation of applicable access/use
rights. External captures are excluded from git and release artifacts;
any future authorized local capture must also satisfy the applicable retention
terms. See [`LICENSING.md`](../../LICENSING.md) and the
[OpenSky terms](https://opensky-network.org/about/terms-of-use).
